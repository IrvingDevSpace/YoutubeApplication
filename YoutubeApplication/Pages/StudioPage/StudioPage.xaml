<Page
    x:Class="YoutubeApplication.Pages.StudioPage.StudioPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:YoutubeApplication.Pages.StudioPage"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="StudioPage"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Page.Resources>

        <!--  YouTube Studio TabItem 樣式  -->
        <Style x:Key="YoutubeStudioTabItemStyle" TargetType="TabItem">
            <Setter Property="Foreground" Value="#AAAAAA" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="MinWidth" Value="90" />
            <Setter Property="Height" Value="56" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid Background="Transparent">

                            <Border x:Name="HoverBackground" Background="Transparent" />

                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                ContentSource="Header" />

                            <!--  底線  -->
                            <Border
                                x:Name="SelectedLine"
                                Height="3"
                                VerticalAlignment="Bottom"
                                Background="White"
                                CornerRadius="2"
                                Visibility="Collapsed" />
                        </Grid>

                        <ControlTemplate.Triggers>

                            <!--  滑鼠移過  -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="HoverBackground" Property="Background" Value="#333333" />
                                <Setter Property="Foreground" Value="#FFFFFF" />
                            </Trigger>

                            <!--  被選取  -->
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="#FFFFFF" />
                                <Setter TargetName="SelectedLine" Property="Visibility" Value="Visible" />
                            </Trigger>

                            <!--  Disabled  -->
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#666666" />
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  TabControl 樣式  -->
        <Style x:Key="YoutubeStudioTabControlStyle" TargetType="TabControl">
            <Setter Property="Background" Value="#282828" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid>

                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <!--  Tab Header 區  -->
                            <Border
                                Grid.Row="0"
                                Background="#282828"
                                BorderBrush="#3A3A3A"
                                BorderThickness="0,0,0,1">

                                <TabPanel
                                    Background="Transparent"
                                    IsItemsHost="True"
                                    KeyboardNavigation.TabIndex="1" />
                            </Border>

                            <!--  內容區  -->
                            <Border Grid.Row="1" Background="#282828">

                                <ContentPresenter ContentSource="SelectedContent" />

                            </Border>

                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>

    <Grid Background="#282828">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  標題  -->
        <TextBlock
            Grid.Row="0"
            Margin="32,28,32,12"
            FontSize="30"
            FontWeight="Bold"
            Foreground="White"
            Text="頻道內容" />

        <!--  Tabs  -->
        <TabControl
            Grid.Row="1"
            Margin="24,0,0,0"
            ItemContainerStyle="{StaticResource YoutubeStudioTabItemStyle}"
            Style="{StaticResource YoutubeStudioTabControlStyle}">

            <TabItem Header="影片">
                <Grid Background="#282828">
                    <TextBlock
                        Margin="16"
                        FontSize="20"
                        Foreground="White"
                        Text="影片內容" />
                </Grid>
            </TabItem>

            <TabItem Header="播放清單">
                <Grid Background="#282828">
                    <TextBlock
                        Margin="16"
                        FontSize="20"
                        Foreground="White"
                        Text="播放清單內容" />
                </Grid>
            </TabItem>

        </TabControl>

    </Grid>
</Page>
