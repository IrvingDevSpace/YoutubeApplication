<UserControl x:Class="YoutubeApplication.Components.VideoCardComponent.VideoCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:YoutubeApplication.Components.VideoCardComponent"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    
    <materialDesign:Card materialDesign:ElevationAssist.Elevation="Dp2"
                         UniformCornerRadius="12"
                         Background="{DynamicResource MaterialDesignPaper}">

        <Button Command="{Binding ClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                CommandParameter="{Binding VideoCard.VideoUrl, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Style="{StaticResource MaterialDesignFlatButton}"
                Padding="0"
                Height="Auto"
                Foreground="{DynamicResource MaterialDesignBody}">

            <Border Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            CornerRadius="8"
                            ClipToBounds="True"
                            Margin="0,0,0,4">
                        <Grid>
                            <Image Source="{Binding VideoCard.ImgSrc, TargetNullValue={x:Null}, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   Stretch="UniformToFill"
                                   RenderOptions.BitmapScalingMode="HighQuality" />

                            <!--<Border Background="#CC000000"
                                    CornerRadius="4"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Bottom"
                                    Padding="4,2"
                                    Margin="8">
                                <TextBlock Text="10:24"
                                           Foreground="White"
                                           FontSize="11"
                                           FontWeight="Medium" />
                            </Border>-->
                        </Grid>
                    </Border>

                    <TextBlock Grid.Row="1"
                               Text="{Binding VideoCard.Title, RelativeSource={RelativeSource AncestorType=UserControl}}"
                               Style="{StaticResource MaterialDesignBody1TextBlock}"
                               FontWeight="SemiBold"
                               TextWrapping="Wrap"
                               MaxHeight="50"
                               TextTrimming="CharacterEllipsis"
                               HorizontalAlignment="Left" />

                    <StackPanel Grid.Row="2"
                                HorizontalAlignment="Left">
                        <TextBlock Text="{Binding VideoCard.ChannelName, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   FontSize="13"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}" />
                        <TextBlock Text="{Binding VideoCard.ViewSummary, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                   FontSize="11"
                                   Foreground="{DynamicResource MaterialDesignBodyLight}" />
                    </StackPanel>
                </Grid>
            </Border>
        </Button>
    </materialDesign:Card>
</UserControl>
