<UserControl x:Class="YoutubeApplication.Components.SearchBarComponent.SearchBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:YoutubeApplication.Components.SearchBarComponent"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">

    <!--<UserControl.Resources>
        <SolidColorBrush x:Key="YoutubeBorderColor"
                         Color="#CCCCCC" />
        <SolidColorBrush x:Key="YoutubeFocusColor"
                         Color="#1C62B9" />
        <SolidColorBrush x:Key="YoutubeButtonBg"
                         Color="#F8F8F8" />
        <SolidColorBrush x:Key="YoutubeButtonHover"
                         Color="#F0F0F0" />
        <Geometry x:Key="SearchIconGeometry">M15.5 14h-.79l-.28-.27C15.41 12.59 16 11.11 16 9.5 16 5.91 13.09 3 9.5 3S3 5.91 3 9.5 5.91 16 9.5 16c1.61 0 3.09-.59 4.23-1.57l.27.28v.79l5 4.99L20.49 19l-4.99-5zm-6 0C7.01 14 5 11.99 5 9.5S7.01 5 9.5 5 14 7.01 14 9.5 11.99 14 9.5 14z</Geometry>

        <Style x:Key="SearchInputBorderStyle"
               TargetType="Border">
            <Setter Property="BorderBrush"
                    Value="{StaticResource YoutubeBorderColor}" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsFocused, ElementName=SearchInput}"
                             Value="True">
                    <Setter Property="BorderBrush"
                            Value="{StaticResource YoutubeFocusColor}" />
                    <Setter Property="BorderThickness"
                            Value="1.5,1.5,0,1.5" />
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="WatermarkStyle"
               TargetType="TextBlock">
            <Setter Property="Visibility"
                    Value="Collapsed" />
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding Keyword, RelativeSource={RelativeSource AncestorType=UserControl}, TargetNullValue=''}"
                                   Value="" />
                        <Condition Binding="{Binding IsFocused, ElementName=SearchInput}"
                                   Value="False" />
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Visibility"
                            Value="Visible" />
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Style="{StaticResource SearchInputBorderStyle}"
                CornerRadius="20,0,0,20"
                Height="40"
                Background="White">

            <Grid Margin="16,0,8,0">
                <TextBlock Text="搜尋"
                           Style="{StaticResource WatermarkStyle}"
                           VerticalAlignment="Center"
                           Foreground="#888888"
                           FontSize="16"
                           IsHitTestVisible="False" />

                <TextBox x:Name="SearchInput"
                         Text="{Binding Keyword, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
                         BorderThickness="0"
                         Background="Transparent"
                         VerticalContentAlignment="Center"
                         FontSize="16"
                         CaretBrush="Black">
                    <TextBox.InputBindings>
                        <KeyBinding Key="Enter"
                                    Command="{Binding SearchCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                    </TextBox.InputBindings>
                </TextBox>
            </Grid>
        </Border>

        <Button Grid.Column="1"
                Command="{Binding SearchCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Width="64"
                Height="40"
                Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="BtnBorder"
                            Background="{StaticResource YoutubeButtonBg}"
                            BorderBrush="{StaticResource YoutubeBorderColor}"
                            BorderThickness="1"
                            CornerRadius="0,20,20,0">
                        <Path Data="{StaticResource SearchIconGeometry}"
                              Fill="#333333"
                              Stretch="Uniform"
                              Width="18"
                              Height="18"
                              VerticalAlignment="Center"
                              HorizontalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver"
                                 Value="True">
                            <Setter TargetName="BtnBorder"
                                    Property="Background"
                                    Value="{StaticResource YoutubeButtonHover}" />
                            <Setter TargetName="BtnBorder"
                                    Property="BorderBrush"
                                    Value="#AAAAAA" />
                        </Trigger>
                        <Trigger Property="IsPressed"
                                 Value="True">
                            <Setter TargetName="BtnBorder"
                                    Property="Background"
                                    Value="#E5E5E5" />
                        </Trigger>
                        <Trigger Property="IsEnabled"
                                 Value="False">
                            <Setter TargetName="BtnBorder"
                                    Property="Opacity"
                                    Value="0.5" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>-->

    <Grid VerticalAlignment="Center">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                BorderBrush="#CCCCCC"
                BorderThickness="1,1,0,1"
                CornerRadius="20,0,0,20"
                Background="White"
                Height="40">
            <TextBox x:Name="SearchInput"
                     Text="{Binding Keyword, RelativeSource={RelativeSource AncestorType=UserControl}, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:TextFieldAssist.DecorationVisibility="Collapsed"
                     materialDesign:HintAssist.Hint="搜尋"
                     materialDesign:HintAssist.IsFloating="False"
                     BorderThickness="0"
                     VerticalAlignment="Center"
                     Margin="16,0,8,0"
                     FontSize="16">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter"
                                Command="{Binding SearchCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" />
                </TextBox.InputBindings>
            </TextBox>
        </Border>

        <Button Grid.Column="1"
                Command="{Binding SearchCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Style="{StaticResource MaterialDesignFlatButton}"
                Background="#F8F8F8"
                BorderBrush="#CCCCCC"
                BorderThickness="1"
                Width="64"
                Height="40"
                Padding="0"
                materialDesign:ButtonAssist.CornerRadius="0,20,20,0">
            <materialDesign:PackIcon Kind="Magnify"
                                     Width="25"
                                     Height="25"
                                     Foreground="#333333" />
        </Button>
    </Grid>
</UserControl>
